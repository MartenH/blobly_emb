# Freestanding host example. All generation + build rules are shared in examples/common.mk so
# every example runs the identical pipeline; this file only points at the repo.
REPO ?= ../..
include $(REPO)/examples/common.mk
